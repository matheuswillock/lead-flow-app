# 🚀 Fluxo de Onboarding para Novos Clientes

## ✅ Implementação Completa

O fluxo de cadastro e assinatura foi atualizado para garantir que todos os novos usuários passem pelo processo de assinatura antes de acessar a plataforma.

## 📋 Fluxo Implementado

```
┌─────────────┐    ┌──────────────┐    ┌─────────────┐    ┌──────────────┐    ┌─────────────┐
│  Landing    │───▶│ Set-Password │───▶│   Sign-In   │───▶│     CRM      │
│   Page      │    │ (Ativação)   │    │   (Login)   │    │  (App)       │
└─────────────┘    └──────────────┘    └─────────────┘    └──────────────┘
```

### 1️⃣ Usuário Acessa Landing Page
- URL: `/`
- Botões:
  - **"Começar Agora"** (hero section) → `/sign-in`
  - **"Começar Agora"** (pricing section) → `/sign-in`

### 2️⃣ Ativação de Conta (Set Password)
- URL: `/set-password`
- Usuário define a senha do usuário provisionado pelo backoffice
- ✅ Após ativar conta, usuário segue para login (`/sign-in`)

### 3️⃣ Escolha do Plano (Subscribe)
- URL: `/subscribe`
- ✅ **NOVO**: Rota agora é **pública** (não requer autenticação)
- Usuário escolhe plano e forma de pagamento:
  - PIX (QR Code)
  - Boleto
  - Cartão de Crédito
- Dados do cadastro são recuperados do `sessionStorage` para prefill

### 4️⃣ Pagamento
- **PIX**: QR Code exibido + Polling para confirmação via webhook
- **Boleto**: Link para impressão + Instruções
- **Cartão**: Processamento imediato

### 5️⃣ Confirmação via Webhook
- Asaas envia webhook para `/api/webhooks/asaas`
- Sistema valida e marca assinatura como `ACTIVE`
- ✅ **NOVO**: Frontend detecta confirmação via polling

### 6️⃣ Redirecionamento para Login
- ✅ **NOVO**: Após confirmação de pagamento, usuário é redirecionado para `/sign-in`
- Mensagem: "Pagamento confirmado! Faça login para acessar sua conta."
- Usuário faz login e é redirecionado para `/{supabaseId}/board`

## 🎯 Comportamento Esperado

### Cenário 1: Usuário novo (provisionado pelo backoffice)

1. **Landing** → Clica em "Começar Agora"
2. **Sign-In** → Se ainda não tiver senha, segue para ativação
3. **Set-Password** → Define senha
4. **Sign-In** → Faz login
5. **CRM** → Acessa `/{supabaseId}/crm`

## 🔒 Segurança e Validações

### Middleware
- ✅ Rotas públicas: `/`, `/sign-in`, `/set-password`
- ✅ Rotas protegidas: `/dashboard`, `/crm`, etc.
- ✅ Webhook routes: Sem autenticação (validação via token)

### Webhook `/api/webhooks/asaas`
- ✅ Validação de token único
- ✅ Verificação de assinatura do payload
- ✅ Atualização de status apenas para pagamentos confirmados

## 📝 Mensagens ao Usuário

### Após Ativar Conta
```
✅ Conta ativada!
Agora faça login para acessar sua conta.
```

### Na Página de Login
- Usuário faz login normalmente
- Após login, é redirecionado para `/{supabaseId}/crm`

## 🧪 Como Testar

### Teste Manual (Ativação + Login)

1. **Ativação (Set Password):**
   - Use a URL de ativação fornecida pelo backoffice para o usuário (rota `/set-password`)
   - Defina uma senha válida

2. **Login:**
   ```bash
   http://localhost:3000/sign-in
   ```
   - Faça login com o e-mail do usuário provisionado
   - Deve redirecionar para `/{supabaseId}/crm`

### Teste Automatizado (Futuro)

```typescript
// test/e2e/onboarding-flow.spec.ts
describe('New User Onboarding Flow', () => {
  it('should activate and login', async () => {
    // 1. Set password (activation link generated by backoffice/invite flow)
    await page.goto('/set-password');
    await fillSetPasswordForm();
    await page.click('[type="submit"]');

    // 2. Login
    await page.goto('/sign-in');
    await fillLoginForm();
    await page.click('[type="submit"]');

    // 3. Verify redirect to CRM
    await expect(page).toHaveURL(/\/.*\/crm/);
  });
});
```

## 🐛 Troubleshooting

### Set-Password não funciona
- ✅ Verificar URL/token gerado no backoffice
- ✅ Verificar logs do servidor

### Usuário não é redirecionado após login
- ✅ Verificar callback/redirect do `signin`
- ✅ Verificar se existe `profile` para o supabaseId

## 📚 Documentação Relacionada

- [NGROK_WEBHOOK_SETUP.md](./NGROK_WEBHOOK_SETUP.md) - Configuração de webhooks locais

## ✅ Checklist de Implementação

- [x] Remover fluxo de Sign-Up
- [x] Ajustar rotas públicas no proxy/middleware
- [x] Testar fluxo completo: set-password → login → crm

---

**Status**: 🟢 **Implementado e Pronto para Teste**
