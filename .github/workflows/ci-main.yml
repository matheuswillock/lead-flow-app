# .github/workflows/ci-main.yml
name: CI (main -> production)

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
    types: [opened, synchronize, reopened]
  # Escotilha de retomada, mesmo padrão de `ci-develop.yml`: sem ela, um deploy
  # de produção que falha só pode ser re-tentado com um commit artificial em
  # `main`. Foi exatamente o que faltou quando produção ficou 3 dias em
  # 16bf5dd3 — não havia como refazer o deploy do SHA que já estava em `main`.
  #
  # `workflow_dispatch` aceita qualquer `--ref`, então os jobs que publicam são
  # travados em `github.ref_name == 'main'`; sem isso, um dispatch a partir de
  # uma feature colocaria aquele ref em produção.
  workflow_dispatch:

concurrency:
  group: ci-main-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  actions: read
  contents: read
  deployments: write

env:
  CI: true
  NEXT_TELEMETRY_DISABLED: 1
  NODE_OPTIONS: --max-old-space-size=5120

jobs:
  ci-light:
    name: CI Light (VPS)
    runs-on: [self-hosted, linux, x64, lead-flow-ci]
    timeout-minutes: 8
    outputs:
      governance-sync: ${{ steps.ci-light-checks.outputs['governance-sync'] }}
      governance-check: ${{ steps.ci-light-checks.outputs['governance-check'] }}
      api-masking-check: ${{ steps.ci-light-checks.outputs['api-masking-check'] }}
      lint: ${{ steps.ci-light-checks.outputs.lint }}
      passed: ${{ steps.ci-light-checks.outputs.passed }}
      tests-result: ${{ steps.ci-light-checks.outputs['tests-result'] }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun & deps
        uses: ./.github/actions/setup-bun-deps

      - name: Run light checks
        id: ci-light-checks
        uses: ./.github/actions/ci-vps-light-checks
        with:
          run-spellcheck: false
          strict-governance: true

  typecheck:
    name: Typecheck
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun & deps
        uses: ./.github/actions/setup-bun-deps

      - name: Typecheck
        env:
          NODE_OPTIONS: "--max-old-space-size=5120"
        run: bun run typecheck

  migration-status:
    name: Migration Status
    if: ${{ (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref_name == 'main' }}
    runs-on: [self-hosted, linux, x64, lead-flow-ci]
    timeout-minutes: 5
    needs:
      - ci-light
      - typecheck
      - build
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: 2.109.1

      - name: Check Migration Status
        shell: bash
        run: |
          set -euo pipefail

          migration_list="$(supabase migration list --db-url "$DIRECT_URL")"
          echo "$migration_list"

          if MIGRATION_LIST="$migration_list" node -e '
            const input = process.env.MIGRATION_LIST ?? "";
            let bad = false;

            for (const line of input.split(/\r?\n/)) {
              if (!/\d{14}/.test(line)) {
                continue;
              }

              const [local = "", remote = ""] = line.split(/[│|]/).map((value) => value.trim());

              if (!local || !remote || local !== remote) {
                bad = true;
                break;
              }
            }

            process.exit(bad ? 1 : 0);
          '; then
            exit 0
          fi

          echo "Migration history is out of sync. Apply the missing migrations or repair the remote history before merging."
          exit 1
        env:
          DIRECT_URL: ${{ secrets.DIRECT_URL }}

  build:
    name: Build
    # Jobs pesados no runner hospedado do GitHub (RAM dedicada por pipeline, paralelo sem OOM na VPS).
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'

      - name: Setup Bun & deps
        uses: ./.github/actions/setup-bun-deps

      - name: Generate Prisma Client
        run: bun run prisma:generate

      # PR: só quality gate — não gera artefato de deploy.
      - name: Build (quality gate)
        if: ${{ github.event_name == 'pull_request' }}
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.SUPABASE_URL || 'https://placeholder.supabase.co' }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY || 'placeholder-anon-key' }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY || 'placeholder-resend-key' }}
          DATABASE_URL: 'postgresql://placeholder:placeholder@placeholder:5432/placeholder'
          DIRECT_URL: 'postgresql://placeholder:placeholder@placeholder:5432/placeholder'
        run: bun run build

      # Push: o MESMO build vira o artefato de deploy.
      #
      # Antes: este job buildava aqui e o `deploy-production` chamava
      # `vercel deploy` sem `--prebuilt` — a Vercel rebuildava do zero em
      # máquina dela, cobrada como Build CPU Minutes (72% da fatura de uso em
      # ago/2026, ~$145/ciclo). Pagávamos para buildar duas vezes.
      #
      # `vercel build` executa o mesmo build command do projeto, então o quality
      # gate é preservado: se o build quebrar, o job falha aqui como antes.
      - name: Vercel build (produção)
        if: ${{ (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref_name == 'main' }}
        run: |
          npx --yes vercel@56.3.2 pull --yes --environment=production --token="$VERCEL_TOKEN"
          npx --yes vercel@56.3.2 build --prod --token="$VERCEL_TOKEN"
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      # `.vercel/output` NÃO é autocontido: cada `.func/.vc-config.json` traz um
      # `filePathMap` (~3.4k entradas por função) e o `vercel deploy --prebuilt`
      # exige que todo caminho ali exista, relativo à raiz do projeto.
      #
      # Levantamento sobre o artefato do run 32593997089 — 21 configs, 4.834
      # arquivos únicos — mostrou que as referências saem de exatamente quatro
      # origens:
      #
      #   node_modules/**        -> restaurado pelo `bun install` do job de deploy
      #   .next/server/**        -> 4.215 refs   <- faltava
      #   .next/<manifests>      -> BUILD_ID, required-server-files.json, ...
      #   .next/node_modules/**  -> symlinks relativos para pacotes externalizados
      #   .env.*.example         -> vêm do checkout
      #
      # Daí `.next` inteiro, não um subconjunto: as três tentativas anteriores
      # falharam uma a uma justamente por listar caminhos à mão
      # (`node_modules/.prisma`, depois `.next/node_modules`, depois
      # `.next/server/chunks`). `tar` também preserva os symlinks, que o
      # `upload-artifact` descarta.
      #
      # `.vercel/project.json` continua de fora: a identidade do projeto vem das
      # env vars, conforme o guardrail de `VPS/GitHub Actions Self-Hosted`.
      - name: Empacotar artefato de deploy
        if: ${{ (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref_name == 'main' }}
        run: tar -czf vercel-deploy.tgz .vercel/output .next

      - name: Upload build artifact
        if: ${{ (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref_name == 'main' }}
        uses: actions/upload-artifact@v4
        with:
          name: vercel-output-${{ github.sha }}
          path: vercel-deploy.tgz
          retention-days: 1
          include-hidden-files: true

  e2e:
    name: E2E (Playwright)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'

      - name: Setup Bun & deps
        uses: ./.github/actions/setup-bun-deps

      - name: Playwright E2E
        uses: ./.github/actions/run-e2e-playwright
        with:
          include-slow: 'true'
          asaas-sandbox-api-key: ${{ secrets.ASAAS_SANDBOX_API_KEY }}

      - name: Upload Playwright report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report
          if-no-files-found: ignore
          retention-days: 7

  deploy-production:
    name: Deploy Vercel (Production)
    if: |
      (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref_name == 'main' &&
      needs.build.result == 'success' &&
      needs['migration-status'].result == 'success' &&
      needs.e2e.result == 'success'
    needs:
      - build
      - migration-status
      - e2e
    runs-on: [self-hosted, linux, x64, lead-flow-ci]
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: vercel-output-${{ github.sha }}

      # `tar -x` restaura `.vercel/output` e `.next/node_modules` com os
      # symlinks intactos, nos mesmos caminhos relativos do build.
      - name: Extrair artefato de deploy
        run: tar -xzf vercel-deploy.tgz

      # `.vercel/output` referencia `node_modules/.prisma/client/client.js` por
      # caminho relativo à raiz do projeto (output tracing do Next). O build roda
      # em `ubuntu-latest` e o deploy aqui, noutra máquina, então esse alvo não
      # existe e o `vercel deploy --prebuilt` aborta com
      # `File does not exist: "node_modules/.prisma/client/client.js"`.
      # Reinstalar as deps + gerar o client restaura o alvo do link.
      - name: Setup Bun & deps
        uses: ./.github/actions/setup-bun-deps

      - name: Generate Prisma Client
        run: bun run prisma:generate

      # Pre-flight: falhar aqui, com mensagem explícita, em vez de deixar o CLI
      # abortar no meio do upload com um erro críptico. Cada item abaixo é um
      # modo de falha já observado ou diretamente adjacente a um.
      - name: Pre-flight do artefato de deploy
        run: |
          set -euo pipefail

          fail() { echo "::error::$1"; exit 1; }

          [ -d .vercel/output ] || fail "artefato ausente: .vercel/output nao foi baixado"
          [ -f .vercel/output/config.json ] || fail "artefato incompleto: .vercel/output/config.json ausente"

          if [ -z "$(find .vercel/output -mindepth 1 -maxdepth 1 -print -quit)" ]; then
            fail "artefato vazio: .vercel/output nao tem conteudo"
          fi

          [ -d .next ] || fail ".next ausente: o artefato nao trouxe o build do Next"

          # Verificação exaustiva: reproduz o que o `vercel deploy --prebuilt`
          # faz ao validar o `filePathMap` de cada função, só que aqui e com o
          # arquivo faltante nomeado. Substitui a lista de caminhos escrita à
          # mão que deixou passar três falhas seguidas — cada uma revelando um
          # caminho que ninguém tinha previsto.
          node -e '
            const fs = require("fs");
            const path = require("path");

            const missing = new Set();
            let configs = 0;
            let refs = 0;

            const walk = (dir) => {
              for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
                const full = path.join(dir, entry.name);
                if (entry.isDirectory()) {
                  walk(full);
                  continue;
                }
                if (entry.name !== ".vc-config.json") continue;
                configs += 1;
                let config;
                try {
                  config = JSON.parse(fs.readFileSync(full, "utf8"));
                } catch {
                  continue;
                }
                for (const target of Object.values(config.filePathMap ?? {})) {
                  refs += 1;
                  if (!fs.existsSync(target)) missing.add(target);
                }
              }
            };

            walk(".vercel/output/functions");
            process.stdout.write(`filePathMap: ${configs} configs, ${refs} refs\n`);

            if (missing.size === 0) return;

            process.stderr.write(`::error::${missing.size} arquivo(s) do filePathMap ausente(s) — o deploy vai abortar\n`);
            for (const file of [...missing].slice(0, 15)) process.stderr.write(`  ${file}\n`);
            process.exit(1);
          '

          echo "Pre-flight OK."

      # `--prebuilt` sobe o `.vercel/output` já pronto em vez de mandar o
      # código-fonte para a Vercel rebuildar. Build CPU Minutes -> ~0.
      #
      # Sem `vercel pull` aqui de propósito: `VERCEL_ORG_ID` + `VERCEL_PROJECT_ID`
      # já identificam o projeto, e `pull` traria as env vars de produção para o
      # disco do runner self-hosted — o guardrail da nota
      # `VPS/GitHub Actions Self-Hosted` pede o contrário.
      #
      # NÃO reintroduzir `2>&1 | tail -1`: sem `pipefail` o shell enxergava o
      # exit code do `tail`, não o do CLI. Dois deploys de produção falharam em
      # silêncio (21/08 capturou a borda `╰──╯` do CLI, 22/08 capturou
      # `File does not exist: ...`) e produção ficou 3 dias atrás de `main` com
      # o job verde. `--meta` carimba o SHA para o passo de verificação abaixo.
      - name: Deploy to Vercel (Production)
        id: vercel-prod
        run: |
          set -euo pipefail

          DEPLOY_LOG="$(mktemp)"
          if ! npx --yes vercel@56.3.2 deploy --prebuilt --prod \
            --token="$VERCEL_TOKEN" \
            --yes \
            --meta githubCommitSha="$GITHUB_SHA" \
            >"$DEPLOY_LOG" 2>&1; then
            echo "::error::vercel deploy falhou (exit code preservado)"
            echo "::group::Vercel CLI — saida completa (deploy falhou)"
            cat "$DEPLOY_LOG"
            echo "::endgroup::"
            exit 1
          fi
          echo "::group::Vercel CLI — saida completa"
          cat "$DEPLOY_LOG"
          echo "::endgroup::"

          DEPLOY_URL="$(grep -oE 'https://[A-Za-z0-9._-]+\.vercel\.app' "$DEPLOY_LOG" | tail -1 || true)"
          if [ -z "$DEPLOY_URL" ]; then
            echo "::error::deploy terminou 0 mas nao imprimiu URL Vercel valida"
            exit 1
          fi
          echo "deploy-url=$DEPLOY_URL" >> "$GITHUB_OUTPUT"
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      # Um `vercel deploy` bem-sucedido não prova que o runtime de produção
      # mudou. Aqui confirmamos, pela API, que o deployment ficou READY, que o
      # alvo é production e que o SHA publicado é o mesmo do commit em `main`.
      - name: Verify production deployment
        id: verify-prod
        run: |
          set -euo pipefail

          DEPLOY_HOST="${DEPLOY_URL#https://}"
          API="https://api.vercel.com/v13/deployments/${DEPLOY_HOST}"
          if [ -n "${VERCEL_ORG_ID:-}" ]; then
            API="${API}?teamId=${VERCEL_ORG_ID}"
          fi

          RESPONSE="$(curl -sS -H "Authorization: Bearer $VERCEL_TOKEN" "$API")"

          RESPONSE="$RESPONSE" EXPECTED_SHA="$GITHUB_SHA" node -e '
            const data = JSON.parse(process.env.RESPONSE ?? "{}");
            const expected = process.env.EXPECTED_SHA ?? "";
            const readyState = data.readyState ?? data.status ?? "";
            const target = data.target ?? "";
            const sha = data.meta?.githubCommitSha ?? "";
            const fail = (message) => {
              process.stderr.write("::error::" + message + "\n");
              process.exitCode = 1;
            };

            // Bloco completo em vez de uma linha: quando um deploy falha, é
            // isto que responde "qual versão está no ar?" sem ter que abrir a
            // Vercel. Diagnosticar os falsos positivos de 21-22/08 custou caro
            // justamente porque o log não guardava nada disso.
            const info = {
              id: data.uid ?? data.id ?? "-",
              url: data.url ? `https://${data.url}` : "-",
              readyState: readyState || "-",
              target: target || "-",
              commitSha: sha || "(sem meta)",
              commitEsperado: expected,
              criadoEm: data.createdAt ? new Date(data.createdAt).toISOString() : "-",
              regiao: (data.regions ?? []).join(",") || "-",
              inspector: data.inspectorUrl ?? "-",
              erro: data.errorMessage ?? data.errorCode ?? "-",
            };

            process.stdout.write("::group::Vercel — deployment de producao\n");
            for (const [key, value] of Object.entries(info)) {
              process.stdout.write(`  ${key.padEnd(14)} ${value}\n`);
            }
            process.stdout.write("::endgroup::\n");

            const summary = process.env.GITHUB_STEP_SUMMARY;
            if (summary) {
              const rows = Object.entries(info).map(([k, v]) => `| ${k} | \`${v}\` |`).join("\n");
              require("fs").appendFileSync(
                summary,
                `\n### Vercel — deployment verificado\n\n| campo | valor |\n|---|---|\n${rows}\n`
              );
            }

            if (readyState !== "READY") fail(`deployment nao esta READY: ${readyState || "desconhecido"}`);
            if (target !== "production") fail(`deployment nao e production: ${target || "desconhecido"}`);
            if (!sha) fail("deployment sem githubCommitSha — nao da para provar a versao publicada");
            else if (sha !== expected) fail(`SHA divergente: publicado=${sha} esperado=${expected}`);
          '
        env:
          DEPLOY_URL: ${{ steps.vercel-prod.outputs.deploy-url }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}

      # Só roda se a verificação reprovou um deployment que chegou a ser
      # promovido. Não cobre o caso em que o deploy nem aconteceu (aí produção
      # simplesmente continua na versão anterior, que é o estado desejado).
      - name: Rollback em verificação reprovada
        if: ${{ failure() && steps.verify-prod.outcome == 'failure' && steps.vercel-prod.outputs.deploy-url != '' }}
        run: |
          set -euo pipefail
          echo "::warning::verificacao reprovou o deployment promovido — revertendo"
          npx --yes vercel@56.3.2 rollback \
            --token="$VERCEL_TOKEN" \
            --yes
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      - name: Production Summary
        run: |
          echo "### Production Deploy (main)" >> $GITHUB_STEP_SUMMARY
          echo "- URL: ${{ steps.vercel-prod.outputs.deploy-url }}" >> $GITHUB_STEP_SUMMARY
          echo "- Branch: main" >> $GITHUB_STEP_SUMMARY
          echo "- Commit: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY

  # Um push em `main` que não termina com produção no SHA do push é um
  # incidente, e o modo mais perigoso é o job de deploy ser **pulado** (o `if:`
  # exige build + migration-status + e2e verdes): nesse caso nada falha e
  # produção fica defasada em silêncio. Foi assim que produção passou 3 dias em
  # 16bf5dd3. Este job roda sempre e é o único que enxerga esse caso.
  deploy-guard:
    name: Deploy Guard (main)
    if: ${{ always() && ((github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref_name == 'main') }}
    needs:
      - deploy-production
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Alertar se produção não ficou no SHA do push
        run: |
          set -euo pipefail

          RESULT="${{ needs.deploy-production.result }}"
          echo "deploy-production: $RESULT"

          if [ "$RESULT" = "success" ]; then
            echo "Deploy concluiu e foi verificado."
            exit 0
          fi

          if [ "$RESULT" = "skipped" ]; then
            MESSAGE="Deploy de producao PULADO no push $GITHUB_SHA — producao continua na versao anterior."
          else
            MESSAGE="Deploy de producao terminou '$RESULT' no push $GITHUB_SHA — producao pode estar defasada."
          fi

          echo "::error::$MESSAGE"

          if [ -n "${SLACK_WEBHOOK_URL:-}" ]; then
            MESSAGE="$MESSAGE" RUN_URL="$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID" \
              node -e '
                const body = JSON.stringify({
                  text: `:rotating_light: *Lead Flow — deploy de produção não concluiu*\n${process.env.MESSAGE}\n${process.env.RUN_URL}`,
                });
                fetch(process.env.SLACK_WEBHOOK_URL, {
                  method: "POST",
                  headers: { "Content-Type": "application/json" },
                  body,
                }).then((r) => process.stdout.write(`slack=${r.status}\n`))
                  .catch((e) => process.stderr.write(`slack falhou: ${e}\n`));
              ' || echo "::warning::nao foi possivel notificar o Slack"
          else
            echo "::warning::SLACK_DEPLOY_WEBHOOK_URL nao configurado — alerta so no Actions"
          fi

          exit 1
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_DEPLOY_WEBHOOK_URL }}
